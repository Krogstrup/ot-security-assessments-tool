[package]
name = "gm-types"
version = "0.1.0"
edition = "2021"
description = "Shared domain constants and models for Kusanagi Kajiki OT/ICS analysis"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
ts-rs = { version = "10" }

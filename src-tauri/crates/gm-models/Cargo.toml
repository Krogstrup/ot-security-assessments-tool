[package]
name = "gm-models"
version = "0.1.0"
edition = "2021"
description = "Shared domain models for Kusanagi Kajiki (asset, connection, packet types)"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
ts-rs = { version = "10" }
